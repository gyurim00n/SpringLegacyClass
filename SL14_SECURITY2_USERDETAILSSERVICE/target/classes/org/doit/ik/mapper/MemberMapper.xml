<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.doit.ik.mapper.MemberMapper">
	<!-- public MemberVO read(@Param("userid")String user) -->
	<resultMap type="org.doit.ik.domain.MemberVO" id="memberMap">
		<id property="id" column="id" />
		<result property="pwd" column="pwd" />
		<result property="name" column="name" />
		<result property="regdate" column="regdate" />
		<!-- 1:N 연관관계 -->
		<collection property="authList" resultMap="authMap">

		</collection>
	</resultMap>
	<resultMap type="org.doit.ik.domain.AuthVO" id="authMap">

		<result property="username" column="username" />
		<result property="authority" column="authority" />
	</resultMap>



	<select id="read" resultMap="memberMap">
		SELECT id, pwd, name, 1 as enabled, id as username,
		regdate, authority
		FROM member m
		LEFT JOIN member_authorities auth ON
		m.id = auth.username
		WHERE m.id = #{ userid }


	</select>



	<!-- public MemberVO getMember(@Param("id") String id) throws ClassNotFoundException, 
		SQLException; -->
	<select id="getMember" resultType="org.doit.ik.domain.MemberVO">
		SELECT *
		FROM member
		WHERE id =
		#{id}
	</select>

	<!-- public int insert(MemberVO member) throws ClassNotFoundException, SQLException; -->
	<insert id="insert">
		INSERT INTO member
		( id, pwd, name, gender, birth,
		is_lunar, cphone, email, habit,
		regdate)
		VALUES( #{id}, #{pwd}, #{name},
		#{gender}, #{birth}, #{is_lunar}, #{cphone},
		#{email}, #{habit},
		SYSDATE)
	</insert>

</mapper>  
 
 
 
 
 